# name: "try { ... } catch(e) { ... } finally { ... }"
# key: tryf
# --
try {
  ${0:`yas-selected-text`}
} catch (${1:e}) {
  ${2:/* handle error */}
} finally {
  ${3:/* be executed regardless of the try / catch result*/}
}